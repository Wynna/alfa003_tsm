//::///////////////////////////////////////////////
// starts an NPC's conversation, but does so privately, so it
//  will not spam up others with the text.
//   -AcadiusLost

void main()
{

ActionStartConversation(GetLastSpeaker(), "", TRUE); 

}